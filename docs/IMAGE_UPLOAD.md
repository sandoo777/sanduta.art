# Image Upload System

## Overview
Система загрузки изображений с локальным хранилищем и возможностью миграции на Vercel Blob Storage.

## 📂 Текущая реализация: Локальное хранилище

### Как работает
- Изображения сохраняются в `/public/uploads/products/`
- Доступны по URL: `/uploads/products/filename.jpg`
- Автоматическое создание директорий
- Проверка типов файлов (только изображения)
- Генерация уникальных имен файлов

### API Endpoint

**POST** `/api/upload`

**Request:**
- Method: `POST`
- Content-Type: `multipart/form-data`
- Body: FormData с полем `file`

**Response Success (200):**
```json
{
  "url": "/uploads/products/1704672000000-image.jpg",
  "size": 123456,
  "type": "image/jpeg",
  "name": "1704672000000-image.jpg"
}
```

**Response Errors:**
- `400`: "No file provided"
- `400`: "Only image files are allowed"
- `500`: "Upload failed"

### Ограничения
- ✅ Отлично для разработки
- ✅ Простая реализация без внешних зависимостей
- ⚠️ **НЕ работает на Vercel/serverless** (файловая система read-only)
- ⚠️ Файлы не в git (см. `.gitignore`)
- ⚠️ Без CDN и оптимизации

## 🚀 Миграция на Vercel Blob Storage (Production)

### Зачем Vercel Blob?
- ✅ Идеально для Next.js на Vercel
- ✅ Простая интеграция
- ✅ Автоматический CDN
- ✅ 100GB бесплатно
- ✅ Serverless-friendly

### Quick Start

1. **Установка**: `npm install @vercel/blob`
2. **Токен**: Vercel Dashboard → Storage → Blob → Create
3. **Env**: `BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxx"`
4. **Код**: См. пример ниже

**Текущая версия**: Локальное хранилище (development)  
**Production**: Мигрировать на Vercel Blob перед деплоем
