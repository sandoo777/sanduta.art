/**
 * CartItemsList - Alias for CartList
 * This component is the same as CartList, exported for compatibility
 * with different naming conventions.
 */

export { CartList as CartItemsList } from './CartList';
